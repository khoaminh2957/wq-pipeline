"""forge — the redesigned alpha engine (docs/redesign/02_design.md).

Pure modules first (no network, no platform state): dsr (robustness maths), signature (novelty),
cells (pyramid targeting). Runner/probe/submitter reuse the existing tools/ infrastructure.
"""
