Parked 2026-09-04 on Khoa's tick after the canary ("viết lại thư viện trước: MATRIX dày trước,
vector sau"): every hypothesis here reads a VECTOR field. The canary measured why they fail as
written (02_design §11b): NaN for names without an event → 3–20-name books → CONCENTRATED_WEIGHT.
They carry the `density` fix but are NOT loaded (load_library reads forge/hypotheses/*.yaml only).
Move a file back up when dense-MATRIX hypotheses have been validated live.
